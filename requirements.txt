# Pinned to the versions deployed in production (Railway deployment e35de580,
# 2026-07-20). Verified: gunicorn 26.0.0 matches the production boot log.
# Phase 0 Sprint 1 — reproducible builds only; no upgrades performed here.
# Upgrades happen only in the annual dependency sprint (Constitution VIII.2).
Flask==3.1.3
requests==2.34.2
gspread==6.2.1
oauth2client==4.1.3
google-genai==2.12.1
gunicorn==26.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
psycopg2-binary==2.9.12
Flask-Login==0.6.3
Werkzeug==3.1.8
cryptography==49.0.0
# Phase 0 Sprint 3: production exception monitoring (disabled unless SENTRY_DSN set)
sentry-sdk[flask]==2.66.0
# Phase RC2.5.4c-x-6f2b-B1a: CSRF foundation, DEPENDENCY ONLY.
# Installed but deliberately UNUSED in this phase: CSRFProtect is not imported
# and not initialised anywhere, so behaviour is byte-identical. Enforcement,
# webhook exemptions and template tokens are B1b/B1c.
# Both are pure-Python wheels requiring Python >=3.10 (runtime.txt pins 3.13.13)
# and pull no new transitive dependency: flask, itsdangerous and markupsafe are
# already present, so adding them changes no existing pin.
Flask-WTF==1.3.0
WTForms==3.2.2
